# Tuple is similar to list that can be store a number of list but we can modify more
#but we can't add new item, we can't remove

numbers = (1, 2, 3, 4)
print(numbers[0])
print(numbers[1])
print(numbers.count(2))
